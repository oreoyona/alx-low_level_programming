#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program
 * @argc - number of arguments
 * arguments of the program
 */
int main(char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

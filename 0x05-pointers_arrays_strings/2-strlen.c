#include "main.h"
/**
 * _strlen - returns the length of a given string
 * @s: the string to work on
 * Return: the lenght of the string
 */
int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != 0; x++)
	{
	}
	return (x);
}

#include "lists.h"

/**
 * list_len - printts the number of nodes of a linked list
 * @h: head of linked list
 * Return :
 */
size_t list_len(const list_t *h)
{
	size_t  count = 0;

	while (h)
	{
		count++;
		h = h->next;
	}
	return (count);
}
